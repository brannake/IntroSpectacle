import axios from "axios";

const API = {
  // Retrieves all quotes from the db
  saveUser: function() {
    return axios.post("/api/signup", { text });
  }
  // Saves a new quote to the db

  };


export default API;
